# ============================================
# PORTALV2 - Environment Variables za Cloud Server
# ============================================
# Kopiraj ovaj fajl u .env na cloud serveru:
# cp env.example.cloud .env
# nano .env

# ============================================
# Node Environment
# ============================================
NODE_ENV=production
PORT=3000

# ============================================
# Database (PostgreSQL)
# ============================================
# Format: postgresql://user:password@host:port/database?schema=public
PRISMA_DB_URL=postgresql://portalv2_user:your-password-here@localhost:5432/portalv2?schema=public

# ============================================
# NextAuth Configuration
# ============================================
# Generiši sa: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://77.77.207.36
# Ili ako imaš domain: https://yourdomain.com

# ============================================
# Client Origin (za CORS i Socket.IO)
# ============================================
NEXT_PUBLIC_CLIENT_ORIGIN=http://77.77.207.36
# Ili ako imaš domain: https://yourdomain.com

# ============================================
# API Gateway Configuration (VAŽNO!)
# ============================================
# Tailscale IP adresa lokalnog računara gde radi API Gateway
# Proveri IP sa: .\api-gateway\get-tailscale-ip.ps1 (na lokalnom računaru)
ERP_API_GATEWAY_URL=http://100.78.79.7:3001

# API Key - MORA BITI ISTI kao u api-gateway/.env fajlu!
# Kopiraj API_KEY iz api-gateway/.env fajla
ERP_API_KEY=your-very-secure-api-key-change-this-in-production

# JWT Secret (opciono - ako koristiš custom JWT secret)
# Ako nije postavljen, koristi se default iz API Gateway-a
# ERP_JWT_SECRET=your-very-secure-jwt-secret-change-this-in-production

# ============================================
# Email Configuration (SMTP) - Opciono
# ============================================
# Ako želiš da šalješ email notifikacije za narudžbe
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ============================================
# Push Notifications (VAPID Keys) - Opciono
# ============================================
# Generiši sa: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here
VAPID_SUBJECT=mailto:admin@italgroup.ba

# ============================================
# Sync API Key - Opciono
# ============================================
# Koristi se za automatsku sinkronizaciju preko cron job-a
# Ako nije postavljen, samo admin može da pokrene sinkronizaciju
SYNC_API_KEY=your-sync-api-key-here

# ============================================
# Cron Secret - Opciono
# ============================================
# Koristi se za zaštitu cron endpoint-a
# Ako nije postavljen, cron endpoint nije zaštićen
CRON_SECRET=your-cron-secret-here

